Collaborative Research & Citation Manager

Backend quickstart
- Create and activate venv: `py -3 -m venv .venv && .venv\\Scripts\\activate`
- Install deps: `pip install -r requirements.txt`
- Configure MySQL in `backend/.env`
- Run migrations: `python backend/manage.py migrate`
- Start server: `python backend/manage.py runserver`


